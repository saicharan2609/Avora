const message = [
  "Stage 6 Group 1 database type generation is wired.",
  "Run the approved Supabase type-generation command when application tables are introduced.",
  "Current generated types represent the empty application-schema baseline.",
].join("\n");

console.log(message);
