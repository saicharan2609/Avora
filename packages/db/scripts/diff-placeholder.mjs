const message = [
  "Stage 6 Group 1 database diff workflow is wired.",
  "No application tables are introduced in this group.",
  "Future schema changes must use reviewed migrations and generated type drift checks.",
].join("\n");

console.log(message);
