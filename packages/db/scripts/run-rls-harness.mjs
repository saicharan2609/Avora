const message = [
  "Stage 6 Group 1 RLS harness wiring is present.",
  "No student-scoped application tables exist yet.",
  "Future student-scoped tables must add deny cases in packages/db/rls/__tests__/.",
].join("\n");

console.log(message);
