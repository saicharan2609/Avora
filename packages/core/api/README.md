
## Stage 8 Group 5 — Academic setup API contracts

Stage 8 Group 5 adds:

- `academic/`

The academic API contracts are transport-facing JSON shapes for later web route handlers.

They must not import `@avora/domain`, `@avora/db`, apps, UI packages, worker packages, AI packages, or retrieval packages.