import type { ReactElement } from "react";
import { Text, View } from "react-native";

export default function IndexRoute(): ReactElement {
  return (
    <View>
      <Text>Avora mobile composition root is running.</Text>
    </View>
  );
}